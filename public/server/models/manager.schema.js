(function() {
    
    module.exports = function(mongoose) {
    
        var managerDiscriminatorSchema = {
            
        };

        var managerDiscriminatorSchemaOptions = {
            
        };

        var ManagerDiscriminatorSchema = mongoose.Schema(managerDiscriminatorSchema, managerDiscriminatorSchemaOptions);

        return ManagerDiscriminatorSchema;
    };
    
})();